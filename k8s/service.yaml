apiVersion: v1
kind: Service
metadata:
  name: air-quality-service
spec:
  # ClusterIP: akses dari luar lewat Ingress (ingress-nginx), bukan LoadBalancer langsung
  type: ClusterIP
  selector:
    app: air-quality
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000